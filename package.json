{
  "name": "firestore-connection-problem",
  "version": "0.1.0",
  "description": "The web version of the interviewer.",
  "private": true,
  "scripts": {
    "pretty": "prettier --write \"./src/**/*.{ts,tsx,json}\"",
    "dev": "vite",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build": "tsc && vite build",
    "build-debug": "tsc && vite build --mode debug",
    "preview": "vite preview"
  },
  "author": "stepstone",
  "devDependencies": {
    "@types/react-dom": "^18.2.4",
    "@vitejs/plugin-react": "^4.0.0",
    "@vitest/coverage-c8": "^0.32.0",
    "prettier": "^2.8.8",
    "typescript": "^5.1.3",
    "vite": "^4.3.9",
    "vitest": "^0.32.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "firebase": "^9.22.2"
  }
}
